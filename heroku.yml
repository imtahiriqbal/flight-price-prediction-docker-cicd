build:
  docker:
    web: Dockerfile
release:
  command:
    # - gunicorn --workers=4 --bind 0.0.0.0:$PORT app:app
    - gunicorn --workers=1 --max-requests=1000 --max-requests-jitter=50 --bind 0.0.0.0:$PORT app:app

run:
  web: gunicorn --workers=1 --max-requests=1000 --max-requests-jitter=50 --bind 0.0.0.0:$PORT app:app

